# 可选：不配也能启动（梦可静默、音乐照常）。推荐启动后在面板右上角「设置 → API 管理」填写，即热生效并自动写入仓库根 .env。

# 文稿：豆包（火山方舟）。变量名必须和 station.config.json 的 llm.apiKeyEnv 一致。
ARK_API_KEY=your-ark-api-key

# 语音：当前默认 MiniMax（station.config.json tts.provider = "minimax"）
MINIMAX_API_KEY=your-minimax-key
MINIMAX_GROUP_ID=your-minimax-group-id
